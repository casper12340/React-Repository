import React__default from 'react';

type KustomerThemeProviderProps = {
    /** The React children elements */
    children: React__default.ReactNode;
    /** The theme to use for the UI, default is the lightTheme */
    theme?: string;
};
/** The React context provider for creating UI's using the Kanvas UI components */
declare const KustomerThemeProvider: React__default.FC<KustomerThemeProviderProps>;

export { KustomerThemeProvider, KustomerThemeProvider as default };
