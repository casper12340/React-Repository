import * as React from 'react';
import { KAppClient, Context } from '@kustomer/apps-client';

type KustomerResponse = {
    /** The KAppClient that you can use to make calls against the server, or get information on the app */
    app: KAppClient;
    /** The context that was passed to the app */
    ctx: Context | undefined;
    /** The data that was passed to the app, only available with data stored in kobjects */
    data?: unknown;
    /** Whether the app is ready to be used */
    isReady: boolean;
};
declare const KustomerContext: React.Context<Partial<KustomerResponse>>;
declare const useKustomerApp: () => Partial<KustomerResponse>;

export { KustomerContext, KustomerResponse, useKustomerApp };
