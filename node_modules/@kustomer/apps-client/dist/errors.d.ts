declare enum Errors {
    NoIDOrOrigin = "could not find (id | origin), please make sure your app is running inside a Kustomer DynamicCard",
    MustInitializeFirst = "you must first initialize the app via `kapp.start()`"
}

export { Errors };
