/**
 * Represents an email address.
 */
type Email = {
    /** The type of email address (e.g. home, work, other). */
    readonly type?: 'home' | 'work' | 'other';
    /** Whether the email address has been verified. */
    readonly verified?: boolean;
    /** The email address. */
    readonly email: string;
};
/**
 * Represents a phone number.
 */
type Phone = {
    /** The type of phone number (e.g. home, mobile, work, fax, other). */
    readonly type?: 'home' | 'mobile' | 'work' | 'fax' | 'other';
    /** Whether the phone number has been verified. */
    readonly verified?: boolean;
    /** The phone number. */
    readonly phone: string;
};
/**
 * Represents a social media channel.
 */
type Social = {
    /** Whether the social media channel has been verified. */
    readonly verified?: boolean;
    /** The user ID associated with the social channel. */
    readonly userid?: string;
    /** The URL of the social media channel. */
    readonly url?: string;
    /** The username of the social media channel. */
    readonly username: string;
    /** The type of social media channel (e.g. facebook, instagram, linkedin, pinterest, twitter). */
    readonly type: 'facebook' | 'instagram' | 'linkedin' | 'pinterest' | 'twitter';
};
/**
 * Represents a URL.
 */
type Url = {
    /** The type of URL (e.g. website, blog, other). */
    readonly type?: 'website' | 'blog' | 'other';
    /** The URL. */
    url: string;
};
/**
 * Represents a location.
 */
type Location = {
    /** The type of location (e.g. home, work, other). */
    readonly type: 'home' | 'work' | 'other';
    /** The name of the location. */
    readonly name?: string | null;
    /** The address of the location. */
    address?: string | null;
    /** The address line 2 of the location. */
    address2?: string | null;
    /** The address line 3 of the location. */
    address3?: string | null;
    /** The latitude of the location. */
    latitude?: number | null;
    /** The longitude of the location. */
    longitude?: number | null;
    /** The two character country code of the location. */
    countryCode?: string | null;
    /** The country name of the location. */
    countryName?: string | null;
    /** The two character state/region/province code of the location. */
    regionCode?: string | null;
    /** The state/region/province name of the location. */
    regionName?: string | null;
    /** The city name of the location. */
    cityName?: string | null;
    /** The postal code of the location. */
    zipCode?: string | null;
    /** The phone number area code of the location. */
    areaCode?: string | null;
};
/**
 * Represents an external ID.
 */
type ExternalID = {
    /** The external ID. */
    readonly externalId: string;
    /** Whether the external ID has been verified. */
    readonly verified?: boolean;
};
/**
 * Represents a Facebook ID.
 */
type FacebookID = {
    /** The Facebook page ID. */
    readonly pageId: string;
    /** The Facebook user ID. */
    readonly userId: string;
    /** The name of this Facebook reference. */
    readonly name?: string;
};
/**
 * Represents an Instagram ID.
 */
type InstagramID = {
    /** The Instagram page ID. */
    readonly pageId: string;
    /** The Instagram thread ID. */
    readonly threadId: string;
    /** The username of this Instagram reference. */
    readonly username: string;
    /** The ID of this Instagram reference. */
    readonly instagramId?: string;
};
/**
 * Represents the sentiment of a customer.
 */
type Sentiment = {
    /** The polarity of the sentiment of the customer (i.e. negative, neutral, or positive). */
    readonly polarity: -1 | 0 | 1;
    /** The confidence of the sentiment of the customer. */
    readonly confidence: number;
};
/**
 * Represents a customer.
 */
type Customer<T = unknown> = {
    /** The revision number of the customer. */
    readonly rev?: number;
    /** The name of the customer. */
    name?: string;
    /** The company of the customer. */
    company?: string;
    /** The external ID of the customer. */
    externalId?: string;
    /** The external IDs of the customer. */
    externalIds?: ExternalID[];
    /** The shared external IDs of the customer. */
    sharedExternalIds?: ExternalID[];
    /** The username of the customer. */
    username?: string;
    /** The avatar URL of the customer. */
    avatarUrl?: string;
    /** The locale of the customer. */
    locale?: string;
    /** The time zone of the customer. */
    timeZone?: string;
    /** The tags associated with the customer. */
    tags?: string[];
    /** The gender of the customer. */
    gender?: 'm' | 'f' | 'o' | 'p';
    /** The emails of the customer. */
    emails?: Email[];
    /** The shared emails of the customer. */
    sharedEmails?: Email[];
    /** The phones of the customer. */
    phones?: Phone[];
    /** The shared phones of the customer. */
    sharedPhones?: Phone[];
    /** The WhatsApp numbers of the customer. */
    whatsapps?: Phone[];
    /** The Facebook IDs of the customer. */
    facebookIds?: FacebookID[];
    /** The Instagram IDs of the customer. */
    instagramIds?: InstagramID[];
    /** The social media channels of the customer. */
    socials?: Social[];
    /** The shared social media channels of the customer. */
    sharedSocials?: Social[];
    /** The URLs of the customer. */
    urls?: Url[];
    /** The locations of the customer. */
    locations?: Location[];
    /** The sentiment of the customer. */
    sentiment?: Sentiment;
    /** The dedicated user of the customer. */
    dedicatedUser?: string;
    /** The custom data of the customer. */
    custom?: T;
    /** The birthday of the customer. */
    birthdayAt?: Date;
    /** The date the customer signed up. */
    readonly signedUpAt?: Date;
    /** The date the customer was imported. */
    readonly importedAt?: Date;
    /** The date of the customer's last activity. */
    readonly lastActivityAt?: Date;
    /** The date of the customer's last customer activity. */
    readonly lastCustomerActivityAt?: Date;
    /** The date the customer was last seen. */
    readonly lastSeenAt?: Date;
    /** The date the customer was created. */
    readonly createdAt?: Date;
    /** The date the customer was last updated. */
    readonly updatedAt?: Date;
    /** The date the customer was last modified. */
    readonly modifiedAt?: Date;
};
/**
 * Represents a customer to be created.
 */
type CustomerCreate<T = unknown> = {
    /** The name of the customer. */
    name?: string;
    /** The company of the customer. */
    company?: string;
    /** The avatar URL of the customer. */
    avatarUrl?: string;
    /** The external ID of the customer. */
    externalId?: string;
    /** The external IDs of the customer. */
    externalIds?: ExternalID[];
    /** The shared external IDs of the customer. */
    sharedExternalIds?: ExternalID[];
    /** The username of the customer. */
    username?: string;
    /** The locale of the customer. */
    locale?: string;
    /** The time zone of the customer. */
    timeZone?: string;
    /** The gender of the customer. */
    gender?: 'm' | 'f' | 'o' | 'p';
    /** The tags associated with the customer. */
    tags?: string[];
    /** The emails of the customer. */
    emails?: Email[];
    /** The phones of the customer. */
    phones?: Phone[];
    /** The WhatsApp numbers of the customer. */
    whatsapps?: Phone[];
    /** The Facebook IDs of the customer. */
    facebookIds?: FacebookID[];
    /** The Instagram IDs of the customer. */
    instagramIds?: InstagramID[];
    /** The social media channels of the customer. */
    socials?: Social[];
    /** The URLs of the customer. */
    urls?: Url[];
    /** The locations of the customer. */
    locations?: Location[];
    /** The custom data of the customer. */
    custom?: T;
    /** The dedicated user of the customer. */
    dedicatedUser?: string;
    /** The birthday of the customer. */
    birthdayAt?: Date;
    /** The date the customer signed up. */
    signedUpAt?: Date;
    /** The date of the customer's last activity. */
    lastActivityAt?: Date;
    /** The date of the customer's last customer activity. */
    lastCustomerActivityAt?: Date;
    /** The date the customer was last seen. */
    lastSeenAt?: Date;
};
/**
 * Represents a customer to be updated.
 */
type CustomerUpdate<T = unknown> = {
    /** The name of the customer. */
    name?: string;
    /** The company of the customer. */
    company?: string;
    /** The avatar URL of the customer. */
    avatarUrl?: string;
    /** The external ID of the customer. */
    externalId?: string;
    /** The external IDs of the customer. */
    externalIds?: ExternalID[];
    /** The shared external IDs of the customer. */
    sharedExternalIds?: ExternalID[];
    /** The username of the customer. */
    username?: string;
    /** The locale of the customer. */
    locale?: string;
    /** The time zone of the customer. */
    timeZone?: string;
    /** The gender of the customer. */
    gender?: 'm' | 'f' | 'o' | 'p';
    /** The tags associated with the customer. */
    tags?: string[];
    /** The emails of the customer. */
    emails?: Email[];
    /** The phones of the customer. */
    phones?: Phone[];
    /** The WhatsApp numbers of the customer. */
    whatsapps?: Phone[];
    /** The Facebook IDs of the customer. */
    facebookIds?: FacebookID[];
    /** The Instagram IDs of the customer. */
    instagramIds?: InstagramID[];
    /** The social media channels of the customer. */
    socials?: Social[];
    /** The URLs of the customer. */
    urls?: Url[];
    /** The locations of the customer. */
    locations?: Location[];
    /** The custom data of the customer. */
    custom?: T;
    /** The dedicated user of the customer. */
    dedicatedUser?: string;
    /** The birthday of the customer. */
    birthdayAt?: Date;
    /** The date the customer signed up. */
    signedUpAt?: Date;
    /** The date of the customer's last activity. */
    lastActivityAt?: Date;
    /** The date of the customer's last customer activity. */
    lastCustomerActivityAt?: Date;
    /** The date the customer was last seen. */
    lastSeenAt?: Date;
};

export { Customer, CustomerCreate, CustomerUpdate, Email, ExternalID, FacebookID, InstagramID, Location, Phone, Sentiment, Social, Url };
